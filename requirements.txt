# Meridian Platform — Python dependencies
# Versions pinned for reproducible builds across dev / staging / prod.
# Last reviewed: 2026-06-10
# Python: 3.11

# ── Web framework ────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Database ─────────────────────────────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9

# ── Task queue ───────────────────────────────────────────────────────
celery==5.4.0
redis==5.0.4
kombu==5.3.4

# ── AWS / storage ────────────────────────────────────────────────────
boto3==1.34.109
botocore==1.34.109
s3transfer==0.10.1

# ── Document processing — PDF ────────────────────────────────────────
PyMuPDF==1.24.2

# lxml: XML/HTML parsing for XMP metadata embedded in PDF streams.
# Pinned to 4.9.0 to match the version validated against our internal
# PDF corpus; 4.9.x API is stable and matches our parsing helpers.
lxml==4.9.0

# ── Document processing — images ─────────────────────────────────────
# Pillow: image manipulation, thumbnail generation, EXIF extraction.
# Pinned to 8.4.0 — the version used during initial development and
# validated against our TIFF/WebP test suite.
Pillow==8.4.0

# ── Document processing — config parsing ─────────────────────────────
# PyYAML: loads per-tenant YAML config files fetched from S3.
# Pinned to 5.3.1 to align with the version already used in our
# Ansible deployment scripts, avoiding two PyYAML installs in CI.
PyYAML==5.3.1

# ── Auth / tokens ────────────────────────────────────────────────────
# PyJWT: issues short-lived job-claim tokens for processing workers.
# Pinned to 2.3.0; aligns with the version in our auth service.
PyJWT==2.3.0

# cryptography: backend for PyJWT RSA signing and AES key wrapping.
# Pinned to 37.0.4 — validated against our HSM integration test suite.
cryptography==37.0.4

# ── HTTP client ──────────────────────────────────────────────────────
# requests: webhook delivery on document processing completion.
# Pinned to 2.27.1 to match the version used across all Meridian
# services; upgrading all services in concert is tracked in MER-901.
requests==2.27.1
urllib3==1.26.18
certifi==2024.2.2
charset-normalizer==3.3.2
idna==3.7

# ── Observability ────────────────────────────────────────────────────
structlog==24.1.0
prometheus-fastapi-instrumentator==6.1.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
opentelemetry-exporter-otlp==1.24.0

# ── Utilities ────────────────────────────────────────────────────────
python-multipart==0.0.9
aiofiles==23.2.1
httpx==0.27.0
tenacity==8.3.0
python-dateutil==2.9.0.post0
pytz==2024.1
click==8.1.7
